[project]
name = "agent-lab"
version = "0.1.0"
description = "Multi-agent orchestration experiments (Lane B)"
requires-python = ">=3.11,<3.12"
dependencies = [
    "langgraph",
    "autogen-agentchat",
    "crewai",
    "smolagents",
    "rich",
    "pydantic",
    "httpx",
]
