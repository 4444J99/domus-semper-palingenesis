# Global AI Agent Instructions

Applies to all AI coding agents (Codex, etc.) operating in this home directory.

{{- $systemInfo := include "dot_config/ai-context/system-info.md.tmpl" }}
{{- $codeStyle := include "dot_config/ai-context/code-style.md.tmpl" }}
{{- $planDiscipline := include "dot_config/ai-context/plan-discipline.md.tmpl" }}
{{ $systemInfo }}
### Plan Paths

- **Project plans**: tool-specific plans directory within each project (e.g., `.codex/plans/`)
- **Global fallback**: `~/.codex/plans/` (only for non-project contexts)

{{ $planDiscipline }}
{{ $codeStyle }}

{{- $universalRules := include "dot_config/ai-context/universal-rules.md.tmpl" }}
{{ $universalRules }}

### Docker Governance

Docker is **not default infrastructure**. Keep Docker Desktop quit; launch only when needed.

**Decision rule:** "How many separate processes need to talk to each other?"
- **One process** → run natively (CLI tool in `~/.local/bin/`, run on-demand)
- **Two+ coordinated processes** (API + DB, app + cache + worker) → Docker Compose
- **Deploying to cloud container runtime** → Docker

**Push back** when asked to use Docker for: single CLI tools, MCP servers, static sites,
or anything that's just one process. Use `npx` or a binary in `~/.local/bin/` instead.
**Never use LaunchAgents** — every incident caused system freezes. On-demand CLI only.

**Accept Docker** when: the project needs a database + API minimum, or already has a
`docker-compose.yml`, or targets ECS/Cloud Run/Kubernetes.
