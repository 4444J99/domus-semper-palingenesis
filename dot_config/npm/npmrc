# Security
audit=true
fund=false

# Performance
prefer-offline=true
progress=false

# Defaults
save-exact=true
engine-strict=false

# Global install prefix (XDG-compliant)
prefix=${XDG_DATA_HOME}/npm

# Init defaults
init-author-name=4jp
init-license=MIT
init-version=0.1.0
