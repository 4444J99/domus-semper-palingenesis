---
name: ai-orchestration-philosophy
description: User's strategic model — Claude + user are the mastermind pair; all other AI services are dispatch capacity for mechanical/tactical work
type: user
---

The user's operating philosophy for multi-agent AI orchestration:

- **Claude + user = strategic brain.** Claude handles architecture, governance, vision, correctness, and audit. The user directs.
- **All other services = worker bees.** Gemini, Codex, OpenCode, Goose handle mechanical and tactical tasks — boilerplate, refactoring, content generation, bulk edits.
- **Goal: maximize token usage across all paid services.** Don't burn Claude Max tokens on work that Gemini or Codex could do safely under constraints.
- **"Keep the onslaught onward"** — the user wants continuous forward momentum. Claude should be doing strategic work, not waiting while boilerplate gets scaffolded.
- This is a force multiplication model, not a fallback model. Don't dispatch to other services only when Claude runs out — dispatch proactively to keep Claude focused on what only Claude can do.
