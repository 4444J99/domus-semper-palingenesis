---
name: Formal ontologist and system-builder
description: User builds ontological systems spanning formal theory AND operational tooling/visualization — uses terse imperative prompts and structured free-form notation — also architects the 8-organ ORGANVM ecosystem
type: user
---

The user uses Claude as both a formalization engine and an implementation partner for ontological system-building. They define foundational concepts — system, primitive, programmable-materia, multiverse — through layered formalization: logic statements → mathematical proofs → algorithmic functions. They also build operational infrastructure (CLI tools, registries, visualizations) to instantiate the formal theory.

**Prompting style:** Terse and imperative for formalization ("define X thru Y > Z > W"). For engineering/design tasks, they use a structured free-form notation with delimiters (`create::::`, `;;;;`, `from-outside::::`, `""""[pasted text]""""`) that layers multiple requirements in a single message. For architectural vision, they present dense poetic/philosophical prose that must be parsed into concrete deliverables — the prose IS the spec, compressed.

**Interaction pattern:** For formalization — prompt IS the spec, no clarifying questions expected. For engineering — they engage with plan mode, approve well-researched plans without changes, and expect the implementation to match the plan. For architectural brainstorming — they want collaborative refinement but resist premature multiple-choice framing; prefer to discover rules empirically from the codebase rather than choose from a menu.

**Broader context:** The user operates the ORGANVM ecosystem — 8 organs (I-VII + META), 105+ repos across 9 GitHub orgs. system-system--system feeds ORGAN-I (Theoria). The workspace manifest is at ~/Workspace/workspace-manifest.json. meta-organvm contains the engine, schemas, ontologia, dashboard, and MCP server.

The formalizations are domain-agnostic by design. The user thinks in recursive, self-referential systems where each concept can nest within or contain any other. They value minimalism and "natural factoring" — the ideal form of a structure is the one closest to its functional essence.
