---
name: Business + bottom-line rule all else
description: Strict bottom-line filter on every action. If it does not directly or near-directly drive income, cut. Refinement, polish, scaffold loyalty, code quality concerns all defer to bottom-line.
type: feedback
originSessionId: 10634ae6-b41b-48b0-99c4-8448527007fe
---
**Rule:** Apply a strict bottom-line filter to every candidate action. Direct income > indirect income > everything else. Refinement, polish, README iteration, cross-link tweaking, and loyalty to existing scaffolds all FAIL the filter.

**Why:** Operator stated verbatim: *"business + bottom-line rule all else"* (2026-05-04). Earlier the operator also said the whole push was about getting away from OCDs/hangups/refinement loops into pure resource acquirement. The bottom-line filter is the discipline that prevents drift into refinement.

**How to apply:**
- Before any build action, write: `Thesis (pain point) / Angles (multi-attack) / Wager (P × payoff = EV) / Cost (session-min) / EV-per-min`
- If EV-per-min < $10, skip
- Hard skip categories regardless of EV-per-min: visual polish, README rewrites for elegance, cross-link variations, naming debates, "what if we ALSO did…"
- Hard take categories: direct cash payouts (bounties, contracts, advisory work), audience acquisition that has a working monetization rail downstream
- When stuck choosing between two actions, take the one with the higher EV-per-min and document why the other was cut
- Polishing the operator's existing 5 products = refinement-coded by default; only revisit if tied to a specific income event
