---
name: feedback_no_deferrals
description: Execute work rather than writing handoff plans — only the user gets to be lazy
type: feedback
---

Do not defer work to "next session" when it can be executed now. Writing a handoff plan instead of running the commands IS laziness. Execute the testament cascade. Update the registry. Write the concordance entries. Add the inquiry log evidence. Do it, don't plan it.

**Why:** The user explicitly said "only I can be lazy." Deferring executable work to a handoff document is the agent equivalent of procrastination. If the commands are known and the data exists, run them.

**How to apply:** When propagating to external indices, run every command that can be run. Only defer when genuinely blocked by missing infrastructure (e.g., companion indices that don't exist yet) — and even then, write the exhaustive construction plan so the blocked item is immediately executable once unblocked.
