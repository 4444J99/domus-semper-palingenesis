---
name: 8-Strata Domain Ideal-Whole Schema
description: Cross-domain substrate generated 2026-04-25 in response to "what makes the ideal whole" question — eight layers + two operators + agent-fleet mapping that hold across radically dissimilar domains (chess/fitness/voodoo). Designed but skill not yet built; this artifact rescues the design from a volatile session export.
type: project
originSessionId: 2026-04-25-evening-design-conversation
---
**What:** The substrate that sits beneath the Product Domain Engine (PDE). PDE wraps the *product* layer (logos/ethos/pathos/kairos for any product-tied-to-domain). This schema is the *domain* layer underneath. Generative for every flag-pierce — Jessica's domain, Matty's, Rob's chess + fitness, the next friend's. Not a curator's framework: the selfish-altruistic loop locks the user into being a real practitioner.

**The 8 strata** (every domain composes from these layers; load distribution shifts by domain):

| # | Layer | Question | Output artifact |
|---|---|---|---|
| 1 | Ontology | What is the domain made of? | `domain-ontology.md` — entities, relations, primitives, vocabulary |
| 2 | Lineage strata | How did it become what it is? | `domain-lineage.md` — history/current/futures × academic/industry |
| 3 | Constellation | Who is doing it well, where do we exist relative? | `domain-constellation.yaml` — 75-person programmatic file (PRT-046 generalizes this) |
| 4 | Gap-map | Where is the unexploited terrain? | `domain-gap-map.md` — market-gap + heist-target list |
| 5 | Practitioner agents | Who serves the work when you show up to set? | `domain-agent-fleet.yaml` — research/capture/production/expert agents |
| 6 | Production stack | What does the work require to ship? | `domain-production-stack.md` — tools, surfaces, distribution |
| 7 | Internal magnet | What flows IN when your flag pierces? | `domain-attractor.md` — refinery rules, transmute through user's voice |
| 8 | External contribution | What flows OUT to the community? | `domain-contribution-charter.md` — open-source quotient, gift declarations |

**Two operators (load-bearing):**

- **Selfish-altruistic loop:** Learn-for-self → Quality-of-product → Service-to-friends → Friends-elevated → Network-effect → Self-elevated → More-learning. Selfishness is a feature: locks user into being practitioner not curator.
- **Magnetic membrane:** Flag pierces → INTERNAL pull (magnetize all applicable, transmute through user's lens, alchemize toward ideal); EXTERNAL push (contribute to community). Two-way; per Tenet Protocol both directions fire simultaneously.

**Three-domain stress test passed:** Chess and fitness/BODI live in the same load profile; voodoo shifts weight to layer 8 (ethical-floor load-bearing) — validates the schema as generative not parochial.

**Agent fleet (layer 5 expanded — the iceberg beneath):**

| Role | Class | Default fleet pick |
|---|---|---|
| domain-ontologist | strategic | Claude |
| domain-historian | research-volume | Perplexity / Gemini |
| domain-constellator | research-curation | Gemini / OpenCode |
| domain-gap-mapper | adversarial | Claude (market-gap-analysis SOP) |
| domain-production-architect | tactical | Codex |
| domain-capture-engineer | mechanical | OpenCode |
| domain-attractor-builder | refinery | Claude |
| domain-contribution-author | publishing | Claude + Gemini |

Trinity-dispatch: Claude masterminds + merges; Codex/Gemini/OpenCode generate volume; user directs vision. When user pierces a flag this swarm dispatches in parallel — user arrives on set with substrate ready (no blocking lighting/audio/capture/research).

**Where:** Design **not yet built**. This artifact + the plan file at `~/.claude/plans/2026-04-25-domain-ideal-whole-substrate-design.md` are the persistence rescue. Source design exists in two volatile untracked exports at `~/Workspace/organvm/organvm-corpvs-testamentvm/2026-04-25-{170910,183936}-local-command-caveat...txt`.

**Project:** Cross-organ — substrate underlies every product-domain pair. Properly belongs in `meta-organvm/praxis-perpetua/` (cross-organ system design canonical home) once formalized as a skill at `~/Workspace/a-i--skills/skills/project-management/domain-ideal-whole-substrate/`.

**For whom:** Anthony's whole agent fleet. Generative for every future flag-pierce.

**State:** Designed, persisted to memory + plan. Skill not built. POC not instantiated. Tracked as IRF-PRT-048.

**Three implementation options (from the design conversation):**
- A — build the substrate skill now (~2-3 sessions; clean separation; generative for every flag-pierce)
- B — manually produce 8 artifacts for chess + fitness as one-offs; abstract once done twice
- C — extend PDE itself (single skill; risks scope creep)

**Recommended sequence (this artifact's revision of the original A→B→C):** persist (this artifact + plan) → track (IRF-PRT-048) → prove (chess constellation POC, ~1-2h) → formalize (skill build, ~2-3h). Reverses agent's recommendation; matches user's "rules first, implementations derive" feedback because the rules need to *exist as artifacts* before they can derive anything.

**Validation against accumulated rules:** rule 36 (seed not specification — minimal generative structure) ✓; rule 35 (rules first, implementations derive) ✓; rule 40 (no nested directory hierarchy — flat `domain-*.{md,yaml}` files at any domain's root) ✓; rule 24 (triadic review — three-domain stress test passed) ✓; rule 49 (dense abstraction — schema doesn't decompose user's intuitions into menus) ✓.

**Cross-references:**
- Sibling: `project_artifact_pde_skill.md` (PDE = product layer; this = domain layer beneath)
- Generalizes: PRT-046 (75-person constellation = layer 3 instance)
- Validated by: PRT-045 cross-pollination diagnosis (BODI/Hokage transplant works because both share substrate)

---

## v2 Addition — Portfolio Operator (2026-04-25 follow-up)

User refined the schema: every flag-pierce isn't just a self-contained vertical — it's a **node in the user's portfolio graph**. This adds a *third* operator alongside the selfish-altruistic loop and magnetic membrane.

**The Portfolio Operator** — three cross-flows fire whenever a domain instantiates:

1. **Meta-skill flow** — transferable practices that exist in every domain become first-class objects: marketing, community-building, voice/writing craft, audience cultivation. User may have low direct interest in friend's primary domain (e.g., Jessica's sad-girl-relationships content) but high interest in the meta-skill substrate. Meta-skills are extractable and reusable across the portfolio.

2. **Domain-pair overlap** — every flag-pierce creates N-1 new conversations: friend-domain × user-domain, friend-domain × every-other-friend-domain. Rob×fitness creates Rob×Hokage cross-pollination (PRT-045 was an instance). Jessica×relationships creates Jessica×Sticks. Each overlap is a publishable artifact opportunity (essay, video, joint product).

3. **Tool/audience cross-flow** — every domain engagement spawns reusable tools (scripts, frameworks, templates) AND audiences. Tools amortize across the portfolio. Audiences cross-pollinate (Sticks borrows Jessica's relationship audience; Hokage borrows BODI's fitness audience).

**Why this is operator-class not layer-class:** layers describe what a domain *has* (ontology, lineage, constellation, etc.); operators describe how *flow* happens. The Portfolio Operator acts on the whole portfolio simultaneously, not inside a single domain. Parallel to the magnetic membrane (single-domain pull/push) and the selfish-altruistic loop (single-engagement self-cycle).

**Stress-test on existing instances:**
- **Rob (chess + fitness):** PRT-045 cross-pollination diagnosis was *already* the Portfolio Operator firing in retrospect — BODI funnel transplanted to Hokage, Hokage premium-content transplanted to BODI. v1 captured it as a one-off synthesis; v2 names the operator that *generates* such syntheses systematically.
- **Jessica (relationships + Sticks):** primary domain low user-interest; meta-skill (writing/voice) high; cross-flow with Sticks app via audience. Three Portfolio Operator outputs per engagement: writing-craft tool extraction + Jessica×Sticks publication + audience-cross-flow plan.
- **Future flag-pierce (Matty + N):** same three outputs auto-generated alongside the 8-layer scaffold.

**Implementation impact on substrate skill (still unbuilt):**
- New mode: `portfolio-resonance` — given a candidate flag-pierce, surfaces the three Portfolio Operator outputs alongside the 8-layer scaffold
- Layer-3 constellation file (PRT-046) gains a column: `cross-domain-overlap-with-other-portfolio-nodes`
- Layer-8 contribution charter gains a section: `tools/assets/audiences that cross-flow back into other portfolio nodes`

**Why this matters now:** v1 schema was generative-per-domain. v2 makes it **portfolio-aware**. Without v2, the substrate could ship 8 great vertical files per friend and still leave the cross-pollination diagnoses (the most valuable artifacts of the entire Rob session) unaccounted-for. The PRT-045 synthesis was *the* breakout insight; v2 makes that breakout systematically generated rather than session-luck.

**Money-flow restatement (user's framing):** "we make money by making other people money." The Portfolio Operator is the mechanism by which one engagement returns multi-fold value: tool extraction + cross-domain artifact + audience cross-flow + meta-skill capital. Single-engagement compensation (client fee) is the smallest pillar; portfolio-level returns dominate over time.

**Schema v2 invariants:** every flag-pierce instantiates the 8 layers (unchanged) AND fires three Portfolio Operator cross-flows (new). Selfish-altruistic loop and magnetic membrane unchanged.
