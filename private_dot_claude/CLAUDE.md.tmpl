# Claude Code Instructions

{{- $systemInfo := include "dot_config/ai-context/system-info.md.tmpl" }}
{{- $codeStyle := include "dot_config/ai-context/code-style.md.tmpl" }}
{{ $systemInfo }}
{{ $codeStyle }}
## Claude-Specific

### Important Paths

- **Claude Plans Directory**: See "Plan File Discipline" below
- **Claude Global Plans Fallback**: `~/.claude/plans/` (only for non-project contexts)

## Plan File Discipline

- Plans MUST be saved in the **project's** `.claude/plans/` directory (e.g., `~/Workspace/my-project/.claude/plans/`), NOT the global `~/.claude/plans/`
- When no project context exists (home dir, ad-hoc tasks), use `~/.claude/plans/` as fallback but with **date-prefixed names**: `YYYY-MM-DD-{descriptive-slug}.md`
- Plans are **never overwritten**. To revise, create a new file with incremented suffix: `YYYY-MM-DD-{slug}-v2.md`
- Agent sub-plans follow: `YYYY-MM-DD-{slug}-agent-{short-id}.md`
- Archive old plans instead of deleting: `~/.claude/plans/archive/YYYY-MM/`

{{- $skillIndex := include "dot_config/ai-skills/skill-index.md.tmpl" }}
{{ $skillIndex }}
