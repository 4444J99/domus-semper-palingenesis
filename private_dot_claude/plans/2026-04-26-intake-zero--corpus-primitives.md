---
title: "Intake Zero — Three-Chat Corpus Decomposition"
subtitle: "First execution of the alchemical-intake constitutional rule (feedback_intake_alchemical_decomposition.md)"
precis: "One question (photography archive → audience) routed through three AIs whose native dispositions match the three intake filters. Subatomic primitives extracted from actual chat content. Photography Tri-State = instance-zero of a universal Domain Ideal-Whole Substrate."
date: 2026-04-26
tags: [SYS, ARC, RES, GEN, IRF-PRT-075, DONE-488, intake, primitives, substrate]
status: draft-artifact
session: hall-monitor-followup
related:
  - feedback_intake_alchemical_decomposition.md
  - feedback_charge_test_and_multiversal_recall.md
  - feedback_sincerity_invariant.md
  - IRF-PRT-071 (auto-flush)
  - IRF-PRT-075 (57-scope drift)
---

# Macro Story

A single question — *use 20K photos to build an audience without doing it for my health* — was routed through three AI tools. The user did not pick the tool; the question's filter level picked the tool. Each chat is the same substrate observed through a different lens.

| AI | Filter | Native disposition | Output shape |
|----|--------|---------------------|--------------|
| Gemini | **Raw abstraction** | Multiversal generation, expansive, narrative | Tri-State Sovereign Artist System, 100-source bibliography, Top-100 profile study, pathos/logos/ethos as biometric stats, Trinity Engine |
| Codex | **Anchor verification** | Forensic path resolution, sqlite/jsonl reads, boundary-defining counts | Session→rollout map, Hokage cluster boundaries, Prima Materia/Atlas count audit (4/68/88), opener/closer line anchors |
| Claude | **Domain execution** | Governance, commits, IRF, hall-monitor closures, sincerity | Statusline propagation, 15-memory mirror live→source, DONE-488 / IRF-PRT-075, commits 70e5ade + 989b898 + 5a88b08 |

The three filters were already named in the user's own taxonomy: *raw abstraction → current-domain formalization → pure idea*. Tool routing is not arbitrary; it's substrate-aware.

# Subatomic Primitives (from actual chat content)

## Entities each chat reified

- **Gemini**: `Archive`(20K photos) · `Vault`(off-platform) · `Window`(IG 9-grid) · `Trinity`(raw+machine+theory) · `Generator`(folder collision) · `Patron`(1000-true-fans) · `Print`(1-of-1 artifact)
- **Codex**: `Rollout`(.jsonl) · `Thread`(sqlite row) · `Anchor`(file:line) · `Lane`(Hokage/Maddie/Achilles) · `Export`(caveat.txt) · `Boundary`(explicit vs adjacent)
- **Claude**: `Memory`(live↔source) · `Vacuum`(IRF-XXX-NNN) · `Hook`(Pre/Post/SessionEnd) · `Substrate`(chezmoi) · `Commit`(additive) · `Mirror`(byte-identical diff)

## Operators (verbs that recur)

| Operator | Gemini form | Codex form | Claude form |
|----------|-------------|------------|-------------|
| Decompose | Reduce to primitive form / Tri-State | Map session → artifact path | Atomize vacuum → IRF |
| Verify | Steel-man / straw-man | Diff filesystem state | Triple-check before close |
| Synthesize | Multiversal collision / Trinity | Cross-reference plans+history | Mirror live → source |
| Persist | Print / Zine / Substack | "session file + export + plan" stack | Commit + push origin |

## Relations

- `subatomic ⊂ atomic ⊂ macro` (recursive containment — the same shape at every scale)
- `surface ≠ identity` (photography, Hokage, Maddie, dotfiles look different; primitives are the same)
- `filter ⟺ tool` (raw→Gemini, anchor→Codex, execute→Claude)
- `local:remote = 1:1` (else soul does not persist)
- `triple-reference` (every entity exists in 3+ locations)

## Lifecycle

```
Question → Raw expansion (Gemini)
         → Anchor verification (Codex)
         → Domain execution (Claude)
         → Artifact persistence (commit/push)
         → Memory mirror (live ↔ source ↔ remote)
         → Closure (DONE-NNN) + Vacuum atomization (new IRF entry)
```

α (question) → ω (closed loop with vacuums logged forward)

## Invariants (carried forward unchanged across all domains)

- N/A is a vacuum — never a resting state
- Nothing local only — `[(local):(remote) = 1:1]`
- Rules are additive — new amends, never overwrites
- Conductor principle — the human directs vision; the system executes
- Plans are artifacts — must be committed and pushed
- Fix bases, not outputs — modify the template, not the rendered file
- Validate before presenting — sincerity (every claim → verifiable referent)
- Charged particles only — chargeless filler dissipates

# Photography Archive = Instance-Zero of Universal Substrate

The Tri-State Sovereign Artist System Gemini synthesized is structurally identical to other domain instances already in the corpus:

| Domain | Locus 1 | Locus 2 | Locus 3 | Synthesizer |
|--------|---------|---------|---------|-------------|
| **Photography** | Cybernetic Philosopher (Machine) | Feral Archivist (Memory) | Architect of Time (Gallery) | The 9-Grid Drop |
| **ORGANVM** | META (Genome) | Theoria (Knowledge) | Ergon (Action) | Praxis-Perpetua |
| **Hokage funnel** | L1 free | L2 email | L3 Discord | L4 premium content |
| **Storefront** | Lexicon (raw) | Persona (mapped) | Render (deployed) | Storefront.config.yaml |
| **Conversation corpus** | RAW intake | BUNDLE federation | ARCHIVE persistence | Surface bundle |
| **Three filters (this conversation)** | Gemini (raw) | Codex (anchor) | Claude (execute) | The pasted braid |

Each row is a `3 + 1` substrate: three loci + one synthesizing operator. The pattern is the substrate; the rows are instantiations.

# Generation Rule-Set

Any new domain instantiates by binding the primitive slots:

```yaml
domain: <name>
substance: <what charged particles fill the archive>
filter_triplet:
  raw: <which AI/process performs expansive generation>
  anchor: <which performs verification>
  execute: <which performs domain commits>
lifecycle:
  alpha: <triggering question>
  omega: <closed loop + vacuums atomized forward>
invariants: [carry-forward-from-universal-list]
```

Photography binding example:

```yaml
domain: photography-archive--tri-state-sovereign
substance: photos[~20000] + journal-entries + generative-collisions
filter_triplet:
  raw: Gemini (Tri-State synthesis, audience theory)
  anchor: file-system (folder dates, Lightroom metadata, exif)
  execute: Claude + IG-API + Substack + custom-vault
lifecycle:
  alpha: "20K photos exist, IG is empty, no audience"
  beta: 9-grid drop (12-post rolling window) + Vault landing
  gamma: Generative Collision Engine (Trinity: photo-A + photo-B + theory)
  delta: Patron ladder ($5/mo Substack → $500 1-of-1 prints)
  omega: Sovereign artist economy, charged particles indexed multiversally
invariants: [N/A=vacuum, local:remote=1:1, additive-only, sincerity]
```

# Cross-References (charged anchors only)

- **Constitutional rule**: `~/.claude/projects/-Users-4jp/memory/feedback_intake_alchemical_decomposition.md` (this plan is its first execution)
- **Closure already shipped**: `DONE-488` in `meta-organvm/organvm-corpvs-testamentvm/INST-INDEX-RERUM-FACIENDARUM.md` (commit `989b898`)
- **Outstanding vacuum**: `IRF-PRT-075` (57-scope drift) — same primitive ("local:remote=1:1") un-instantiated for 57 other scopes
- **Chronos-keeper**: `~/.claude/plans/2026-04-25-titan-keeper-architecture.md` (the auto-flush hook that would mechanically prevent re-drift)
- **Substrate skill**: `~/Workspace/a-i--skills/skills/project-management/domain-ideal-whole-substrate/SKILL.md` (the formalization layer beneath PDE)

# Vacuums opened by this artifact

- `IRF-NEW-1` — Mirror this plan to chezmoi source (`~/Workspace/4444J99/domus-semper-palingenesis/.claude/plans/...` if tracked there, else `private_dot_claude/plans/`). Same `local:remote=1:1` invariant applies to plans, not just memories.
- `IRF-NEW-2` — Photography Tri-State binding above is unbuilt as code. Specced here only.
- `IRF-NEW-3` — The "tool ⟺ filter" routing rule should become a hook or a Conductor dispatch rule. Currently implicit human routing.

# Safe to close

Plan persisted. Artifact lives at `~/.claude/plans/2026-04-26-intake-zero--corpus-primitives.md`. Mirroring + commit pending user authorization (per global CLAUDE.md commit-safety guidance).
