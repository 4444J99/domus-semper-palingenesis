# 2026-04-25 — Achilles Full Gap-Closure Implementation Plan

## Context

This is the implementation companion to
`2026-04-25-achilles-master-attack-and-victory.md`.

The user asked for a **full implementation plan for closing every gap** after
the relay-verification tail exported at:

- `/Users/4jp/2026-04-25-195606-local-command-caveatcaveat-the-messages-below.txt`

## Double-Check Against the Original Export

The export is only **48 lines**. It does not contain hidden asks beyond:

1. relay verification already completed
2. two vacuums already atomized
3. the missing request: *"Make a plan for a full, complete, exhaustive attack
   and victory."*

So the canonical gap inventory is not the export alone. It is the export plus
the artifacts the prior session explicitly said it was about to read:

- `2026-04-25-achilles-session-workload.md`
- `2026-04-25-relay-verification-achilles-handoff.md`
- `i-need-you-to-merry-thimble.md`
- `2026-04-25-relay-sys-156-ci-cascade.md`
- `2026-04-25-hanging-items-full-implementation-plan.md`
- vacuum memories for the unsourced CI count and undefined ID authority

**Conclusion:** the existing Achilles master plan is directionally correct, but
it is strategic. What was still missing was an implementation-grade closure map
for each surviving gap. This file is that map.

## Scope Contract

This plan closes **every gap in the Achilles stack** and **routes every gap
outside the Achilles lane**.

That distinction matters:

- If a gap belongs to Maddie or Rob's active code lane, Achilles closes the
  **tracking/ownership gap**, not the code.
- If a gap is user-gated, Achilles closes the **prework + unblock packet** so
  the only remaining step is the user's action.
- If a gap is collaborator-gated, Achilles closes the **support artifact +
  reminder state** and waits cleanly.

## Routed Out of Achilles

These remain real gaps, but they are not Achilles implementation targets:

### RX-01 — Maddie / spiral lane

Source: `i-need-you-to-merry-thimble.md`

Includes:
- IconWorld physics wiring
- cohesion regime
- chaos regime
- `envVar` on `SpiralNode`
- `naming-chains.ts`
- `/lineage` route/page
- commit/push of the spiral working tree

**Achilles closure mode**
- verify relay + plan + memory point at the right repo and owner lane
- do not edit `sovereign-systems--elevate-align/`

### RX-02 — Rob / Hokage / BODI lane

Source: `2026-04-25-hanging-items-full-implementation-plan.md`

Includes:
- Hokage deploy/domain/funnel work
- Discord/community/funnel architecture
- Rob review loops
- BODI cross-pollination execution

**Achilles closure mode**
- keep blockers and next actions tracked
- do not poach `4444J99/hokage-chess/`

## Achilles-Owned Gap Register

| ID | Gap | Source | Closure artifact |
|---|---|---|---|
| AG-01 | PRT-048 skill unbuilt | workload + audit | shipped skill in `a-i--skills` |
| AG-02 | SYS-156A humans-waiting still fuzzy | workload + CI relay | exact user packet + drained autonomous items |
| AG-03 | stale `~890 failures / 14 repos` scope | verification vacuum | sourced snapshot ledger |
| AG-04 | dotfiles CI not root-caused | CI relay | evidence-backed fix or blocker |
| AG-05 | broader CI cascade not triaged | CI relay | repo-by-repo taxonomy + fixes |
| AG-06 | PRT/SYS/DONE authority undefined | verification vacuum | located authority store or new design spec |
| AG-07 | IRF hygiene deferred | workload | adjudication pass + updated references |
| AG-08 | multi-lens viewer has no design artifact | workload + audit | dated plan file |
| AG-09 | conversation-engine residuals still open | hanging-items plan | CCE fixes + verify ingest path |
| AG-10 | resolve-bootstrap / resolve-audit gap | hanging-items plan | wired resolver path or exact blocker packet |
| AG-11 | email triage redesign missing | workload + hanging-items plan | Apps Script/event-driven spec + implementation |
| AG-12 | AI export consolidation blocked | workload | alternative ingest path/spec |
| AG-13 | OSS contribution close-out debt | workload | stagger-safe completion sweep |
| AG-14 | cloudflared root cause unknown | workload | diagnosis artifact + tested fix/workaround |
| AG-15 | plugin marketplace follow-through | workload | final hook/marketplace closure |
| AG-16 | iCloud + TM user-gated infra debt | workload + hanging-items plan | preflight docs + exact user packet |
| AG-17 | CUE declaration layer not started | workload | Phase 1 implementation |
| AG-18 | institutional substrate Phase 0 not started | workload | Phase 0 implementation |
| AG-19 | organ identity / morphogenesis / domain architecture unresolved | workload | routing/design updates |
| AG-20 | atom + metrics backlog lacks active adjudication | workload | prioritized triage pass |
| AG-21 | zeitgeist thesis lacks research pipeline | workload | research program artifact |

## Closure Rules by Gap Type

### For build gaps

Closure means code/files exist, validate, commit, and push.

### For design gaps

Closure means a dated plan/spec exists in the owning repo, is mirrored into
tracked state, and names the next implementation step.

### For user-gated gaps

Closure means:
- all autonomous prep is done
- a single exact user packet exists
- once the user acts, no rediscovery is needed

### For collaborator-gated gaps

Closure means:
- support artifacts are delivered
- the wait state is recorded with next-contact timing

## Phase Sequence

### Phase 1 — Reconcile evidence and remove fake uncertainty

Order:

1. **AG-03** — replace the stale CI number with a fresh sourced ledger
2. **AG-02** — split SYS-156 into the actual humans-waiting packet
3. **AG-06** — locate or define PRT/SYS/DONE authority
4. **AG-07** — only then do IRF hygiene

**Why first:** these gaps distort decision-making for everything downstream.

### Phase 2 — Ship the highest-leverage substrate

5. **AG-01** — build PRT-048 completely
6. **AG-08** — write the multi-lens viewer design artifact

**Why second:** PRT-048 is the biggest unbuilt approved deliverable; the
multi-lens system is major unrepresented intellectual work from the transcript.

### Phase 3 — Collapse the active system failure surface

7. **AG-04** — dotfiles CI
8. **AG-05** — remaining CI cascade
9. **AG-10** — resolver/bootstrap wiring
10. **AG-09** — conversation-engine residuals

### Phase 4 — Close operational debt

11. **AG-11** — email triage redesign
12. **AG-12** — AI export consolidation
13. **AG-13** — OSS contribution close
14. **AG-14** — cloudflared root cause
15. **AG-15** — plugin marketplace follow-through
16. **AG-16** — iCloud/TM preflight packets

### Phase 5 — Close architectural debt

17. **AG-17** — CUE declaration layer
18. **AG-18** — institutional substrate Phase 0
19. **AG-19** — organ identity / morphogenesis / domain architecture
20. **AG-20** — atoms + metrics prioritization
21. **AG-21** — zeitgeist thesis program

## Detailed Implementation Plan

### AG-01 — Build PRT-048

Repo:
- `~/Workspace/a-i--skills/`

Steps:
1. Read the PDE skill and PRT-048 design side-by-side.
2. Scaffold `skills/project-management/domain-ideal-whole-substrate/`.
3. Write `SKILL.md` with the 8 strata + 2 operators + invocation law.
4. Create `assets/` templates for each stratum.
5. Create `scripts/` for dispatch/audit helpers.
6. Validate that no placeholders/TODO shells remain.
7. Commit and push in `a-i--skills`.

Verification:
- directory exists
- skill docs are coherent end-to-end
- referenced scripts/assets exist

### AG-02 — Drain SYS-156A humans-waiting

Repo/surface:
- GitHub notifications + memory/plan artifacts

Steps:
1. Pull only participating/non-CI items.
2. Separate real-human items from Dependabot review requests.
3. Resolve every autonomous item immediately.
4. Write one exact packet for the truly user-owned items.
5. Update the earlier wording so "bulk archive" is no longer implied.

Verification:
- remaining queue reduced to an exact short list
- each remaining item names owner, decision, and consequence

### AG-03 — Replace the stale CI scope with a real ledger

Repo/surface:
- authority layer to be chosen by AG-06; interim tracked artifact allowed

Steps:
1. Re-run the unread notification snapshot if needed.
2. Re-run per-repo failure bucketing.
3. Persist the snapshot to a tracked dated artifact.
4. Reference that artifact from memory or IRF so the number has a receipt.

Verification:
- no future plan cites `~890 / 14 repos` without replacement
- one file on disk names the sourced counts, timestamp, and repo breakdown

### AG-04 — Root-cause dotfiles CI

Repo:
- `~/Workspace/4444J99/domus-semper-palingenesis/`

Steps:
1. Inspect the most recent failed `Lint & Validate` runs.
2. Classify failure cause: workflow drift, lint rule, env mismatch, bad file.
3. Fix the base in the source repo.
4. Re-run local checks if possible.
5. Push the fix.

Verification:
- one real failing run is explained from evidence
- fix is committed or blocker is named precisely

### AG-05 — Triage the remaining CI cascade

Repos:
- top-failure repos from the sourced ledger

Steps:
1. Attack in descending failure count.
2. Group failures by shared cause.
3. Dispatch mechanical fixes only after taxonomy is known.
4. Push fixes repo-by-repo.
5. Mark-read only after remediated repos are genuinely green or retired.

Verification:
- top repos have named cause buckets
- at least the highest-volume cluster is reduced materially

### AG-06 — Resolve ID authority

Repos/surfaces:
- `a-organvm/`, `organvm-corpvs-testamentvm/`, ledgers, GH issue space

Steps:
1. Search for the canonical storage of PRT/SYS/DONE IDs.
2. If found, document the authority path.
3. If not found, write a design/spec for a real authority store.
4. Update Achilles artifacts to stop pretending the authority already exists.

Verification:
- either a file/system is named as authority
- or a dated design file exists that defines the missing authority layer

### AG-07 — Perform IRF hygiene

Prereq:
- AG-06 complete enough that IDs have meaning

Steps:
1. Adjudicate the 12 PRT items.
2. Resolve fossil-record drift.
3. Route the orphan transcript.
4. Update references from plans/memory where stale.

Verification:
- each adjudicated item has a stable pointer
- no hygiene step relies on memory-only IDs unknowingly

### AG-08 — Write the multi-lens viewer plan

Repo/path:
- project-local dated plan artifact in the appropriate repo or dotfiles plan set

Steps:
1. Preserve the journal/Siddhartha origin explicitly.
2. Define the data model separately from Maddie's site.
3. Define lenses, composition rules, and reader UX.
4. Define how it relates to envVar/naming-chain substrate without collapsing
   into that implementation.

Verification:
- one dated plan exists
- it names implementation targets rather than only philosophical framing

### AG-09 — Close conversation-engine residuals

Repo:
- `~/Workspace/organvm/conversation-corpus-engine/`

Steps:
1. Build the json-per-conversation adapter from hanging-items B2.
2. Fix `thread_path` for multi-part imports.
3. Add `--throttle` to standalone import CLI.
4. Verify whether the 2026-04-23 ingest must be rerun.

Verification:
- code/tests pass for the touched paths
- ingest path is either validated or rerun deliberately

### AG-10 — Resolve the resolver gap

Repo/surface:
- dotfiles resolver/bootstrap tooling

Steps:
1. Run the audit path the hook expects.
2. Distinguish live-path findings from true violations.
3. Wire the bootstrap/audit layer so future sessions stop reopening the same
   false gap.
4. If user action is unavoidable, produce one exact unblock packet.

Verification:
- either the audit signal is corrected
- or the user packet is precise enough that the gap is no longer fuzzy

### AG-11 — Redesign email triage

Steps:
1. Treat LaunchAgent architecture as dead.
2. Design event-driven Gmail Apps Script flow.
3. Implement labels/rules/actions in a non-freezing model.
4. Test against a bounded inbox slice.

Verification:
- no LaunchAgent dependency remains
- one working event-driven path exists

### AG-12 — Unblock AI export consolidation

Steps:
1. Reconfirm Gemini Takeout failure mode.
2. Evaluate alternatives: Takeout API path, Drive pipeline, browser export,
   transformation tooling.
3. Choose one reproducible ingest path and document it.

Verification:
- one non-hand-wavy path is named and tested or clearly selected

### AG-13 — Close OSS contribution debt

Steps:
1. Inventory the 21 PRs and 3 LinkedIn drafts.
2. Sort by waiting-on-human vs autonomous close.
3. Respect stagger/comment reputation rules.
4. Finish the sweep or produce the exact residue list.

Verification:
- every open contribution thread has a named next action

### AG-14 — Diagnose cloudflared

Steps:
1. Reproduce the failing network conditions.
2. Compare quick tunnel behavior vs ngrok workaround.
3. Test the likely root-cause axes: NAT, IPv6, hotspot, origin reachability.
4. Write the diagnosis and chosen operating mode.

Verification:
- one root-cause hypothesis is evidence-backed
- or a stable operational workaround is blessed explicitly

### AG-15 — Finish plugin marketplace follow-through

Steps:
1. Re-read the prior fix state.
2. Verify hook `if` behavior and marketplace config now.
3. Close any remaining regression or sync gap.

Verification:
- plugin/hook path behaves as intended from source of truth

### AG-16 — Prepare iCloud + Time Machine user packets

Steps:
1. For iCloud, document exactly what Apple-side reset request is needed.
2. For TM SSD, produce the conversion checklist, staging requirements, and
   rollback plan.
3. Stage any autonomous prep.

Verification:
- user receives one packet per user-gated system task
- no rediscovery is needed later

### AG-17 — Implement CUE Phase 1

Steps:
1. Re-open the research and repo unification context.
2. Choose the first constrained conversion slice.
3. Implement validation path and commit.

Verification:
- at least one real `system.toml` surface has a corresponding CUE path

### AG-18 — Implement institutional substrate Phase 0

Steps:
1. Re-open the 19 primitives / 4 operators / 4 formations spec.
2. Build the Phase 0 data model or CLI scaffold.
3. Commit the first executable substrate.

Verification:
- Phase 0 exists as code, not only prose

### AG-19 — Close architecture identity gaps

Includes:
- organ morphogenesis
- 4444J99 organ identity
- 6-domain / 3-circle domain architecture

Steps:
1. Reconcile the naming/routing law.
2. Decide what moves, what stays, and what becomes authoritative.
3. Persist the resulting architecture artifact(s).

Verification:
- at least one authoritative routing artifact exists for the identity layer

### AG-20 — Triage atoms and metrics backlog

Steps:
1. Prioritize before atomizing more.
2. Run a judgment pass on the highest-value open atoms.
3. Surface real execution clusters instead of backlog inflation.

Verification:
- a prioritized subset exists with next actions

### AG-21 — Build the zeitgeist research program

Steps:
1. Turn the thesis into a research pipeline, not an aspiration.
2. Define corpus, questions, outputs, and dispatch pattern.
3. Persist the research program artifact.

Verification:
- one dated artifact turns the thesis into executable research work

## Implementation Order for the Next Three Achilles Sessions

### Session A

- AG-03
- AG-02
- AG-06
- AG-01 start

### Session B

- AG-01 finish
- AG-04
- AG-05 first cluster
- AG-07

### Session C

- AG-08
- AG-09
- AG-10
- AG-11 start

Everything after Session C remains in the queue, but by then the system will
have stopped lying about scope and the highest-leverage substrate will exist.

## Definition of Done

This plan is complete when:

- every Achilles-owned gap above is either built, specified, or reduced to an
  exact user/collaborator packet
- every routed-out gap has explicit owner-lane tracking
- all resulting artifacts are committed, pushed, and mirrored into memory

## Relationship to Existing Achilles Files

- `2026-04-25-achilles-session-workload.md` = v1 workload capture
- `2026-04-25-achilles-master-attack-and-victory.md` = strategic ordering
- `2026-04-25-achilles-full-gap-closure-implementation.md` = implementation
  companion that closes every remaining gap in the Achilles stack
