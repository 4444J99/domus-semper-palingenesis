# Praxis — Remediation Plan for `fluttering-exploring-token.md`

**Status**: PENDING_EXECUTION (transitioning to ACTIVE during session-close)
**Dialect**: FORMAL_LOGIC + RHETORIC
**σ-position**: 0 → 0 (action stays at substrate-operational scale)

## 1. Forward Plan — protocol-derived, not ad-hoc

User correction 2026-05-02 (twice): *"protocols dictate actions; reuse+reinvent."* Action sequences must be derived from the substrate's existing SOP library (121 active SOPs / 64 in praxis-perpetua/standards/) before any ad-hoc enumeration.

## 2. Relevant SOPs that dictate this plan's execution path

| SOP | Dictates |
|---|---|
| `theory-to-concrete-gate` | The gate-crossing from research/synthesis to implementation; passed at Level 3 |
| `session-self-critique` | 7-phase session-close protocol (Inventory → Triage → Audit → Lessons → Reconciliation → Concrete-Output Gate → Context Budget) |
| `research-to-implementation-pipeline` (The Gold Path) | Named pipeline for research → implementation |
| `recursive-study-feedback` (The Ouroboros) | Ongoing recursive study + feedback during execution |
| `atomic-clock` | Cadence/kairos timing of action firing |
| `execution-sequence` | Order of operations |
| `promotion-and-state-transitions` | LOCAL → CANDIDATE → ... lifecycle for resulting artifacts |
| `theoretical-concept-versioning` (The Epistemic Protocol) | How concepts version as substrate evolves |

## 3. Specific Praxis Vectors

These are *constraints* on whatever actions the SOPs dictate, not action substitutes:

- *Energy-amortization*: every artifact produces all four faces in one token-spend
- *Identity-protection*: each substrate-touching action declares which `ent_*` UID it affects and whether stabilizing or destabilizing
- *Anti-entropy*: target Δ24h ≥ 0% by closing GHOST counterparts faster than creating new implementations
- *VISION.md vacuum*: open IRF entry to flag the empty master-North-Star
- *Protocol-first*: before any action enumeration, consult `organvm sop lifecycle` to identify the dictating SOP(s)

## 4. Order of Operations (per session-self-critique Phase V)

This session-close (in execution now):
1. ✓ Save 6 memory files
2. ✓ Update MEMORY.md index
3. ✓ Split tetradic counterparts to separate files (this set)
4. Open IRF entry for VISION.md vacuum
5. Write session log to praxis-perpetua/sessions/
6. Update praxis-perpetua/lessons/derived-principles.md additively
7. Commit all changes (advances Level 3 → Level 4)
8. Run domus-memory-sync to push memory through chezmoi
9. Push commits to remotes (per Universal Rule #2)

Future sessions, each with their own theory-to-concrete-gate verification:
- Session N+1: Phase 0 environment setup (output: conda env + conventions doc committed)
- Session N+2: Lane 1 first notebook (output: notebook + HTML artifact committed)
- Session N+3: Lane 6 graduation infra (output: organvm CLI subcommand committed)

Each session bounded by its own gate. No session attempts everything.
